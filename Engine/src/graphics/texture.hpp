#pragma once
#include "../resources/surface.hpp"

/*
namespace ns
{
	//Rappresenta una immagine nella GPU
	class texture
	{
	public:
		texture();
		texture(surface* surface);

		~texture();

	private:
		//Dimensione e numero di bit per pixel
		i32 _width, _height, _bpp;
		//Handle di opengl
		u32 _gl_handle;
	};
}
*/