#pragma once
#include "../common.hpp"

#include "../utils/path.hpp"
#include "../utils/io.hpp"

/*
namespace ns
{
	enum class subshader_type
	{
		null,
		vertex, 
		pixel
	};

	//Programma che gira sulla GPU
	class shader
	{
	};

	//Carica shaders risolvendo le macro
	class shader_factory
	{
	public:
		//Aggiunge file risorsa
		void add_common_file(path filepath);

		//Genera source e crea shader
		shader* load_from_file(path filepath);

	private:
		//File includibili tramine "#include" negli shaders
		unordered_map<string, string> _commons;
	};	
}
*/